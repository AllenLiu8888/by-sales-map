# ==============================================
# Universal Deployment Configuration
# ==============================================

# --- Server Connection ---
SERVER_IP="8.141.117.241"
SERVER_USER="root"
SSH_KEY_PATH="/Users/allen/AliCloud.pem"

# --- Project Details ---
# The domain name for Nginx configuration
DOMAIN_NAME="8.141.117.241"

# The port Nginx should listen on (80 for standard HTTP, or 8080, etc.)
NGINX_PORT=8081

# Project Type: "static" (Frontend only) or "fullstack" (Node.js Backend + Frontend)
PROJECT_TYPE="static"

# --- Git Configuration ---
# The HTTP URL of your GitHub repository
REPO_URL="https://github.com/AllenLiu8888/by-sales-map.git"
# The branch to deploy
BRANCH="main"

# --- Database (Fullstack Only) ---
# Leave empty if not using a database
DB_NAME="my_app_db"
DB_USER="my_app_user"
DB_PASSWORD="secure_password"

# --- Build Configuration ---
# Directory where the frontend build output is located (relative to project root/client)
FRONTEND_DIST_DIR="dist"
# Port your backend application runs on (Fullstack only)
APP_PORT=3000
